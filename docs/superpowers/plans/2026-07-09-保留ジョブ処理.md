# 保留ジョブ処理（中断に強い後処理）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 「停止してノート化」を押したら即座にフタを閉じて移動でき、未完の文字起こし・要約はアプリ起動時／スリープ復帰時に自動で続きから処理する。

**Architecture:** 録音フォルダ内のファイルの有無（`録音.wav`／`文字起こし.txt`／`ノート.md`）で処理段階を判定し、別キューDBを持たずに再開可能にする。停止処理を非ブロッキング化し、重い文字起こし（ローカル）と要約（ネット必須）はバックグラウンドの逐次ジョブ処理に一本化する。トリガはアプリ起動時と `powerMonitor` の `resume`。

**Tech Stack:** TypeScript (ESM, `.js` import 指定子), Electron, Node.js fs, Vitest, mlx_whisper, claude/ollama。

## Global Constraints

- TypeScript は型を明示し `any` を避ける。コメントは日本語。
- import 指定子は `.js` 拡張子付き（ESM）。
- テストは Vitest。ロジック層（純関数・fs ユーティリティ）を優先し、Electron の UI 副作用（`Tray`/`Notification`/`powerMonitor`）は対象にしない。
- ファイル名定数は既存の保存名に一致させる: 音声 `録音.wav`、整形済み文字起こし `文字起こし.txt`、ノート `ノート.md`。
- ファイル書き込みは `atomicWriteFile`（tmp→rename）を使う。
- コミットメッセージは日本語・命令形。各コミットに `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` を付ける。
- `commit`/`push` はユーザー指示時のみ。本計画の各「Commit」ステップはユーザーが実行を許可した場合に行う。

---

### Task 1: 段階判定 `nextStage`（純関数）

**Files:**
- Create: `src/pipeline/pendingJobs.ts`
- Test: `src/pipeline/pendingJobs.test.ts`

**Interfaces:**
- Produces:
  - `type NextStage = "transcribe" | "summarize" | "skip"`
  - `function nextStage(files: { hasAudio: boolean; hasTranscript: boolean; hasNote: boolean }): NextStage`

- [ ] **Step 1: 失敗するテストを書く**

`src/pipeline/pendingJobs.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { nextStage } from "./pendingJobs.js";

describe("nextStage", () => {
  it("音声が無ければ skip（対象外）", () => {
    expect(nextStage({ hasAudio: false, hasTranscript: false, hasNote: false })).toBe("skip");
    expect(nextStage({ hasAudio: false, hasTranscript: true, hasNote: false })).toBe("skip");
  });

  it("ノートが有れば skip（完了）", () => {
    expect(nextStage({ hasAudio: true, hasTranscript: true, hasNote: true })).toBe("skip");
    expect(nextStage({ hasAudio: true, hasTranscript: false, hasNote: true })).toBe("skip");
  });

  it("音声のみ（文字起こし未）なら transcribe", () => {
    expect(nextStage({ hasAudio: true, hasTranscript: false, hasNote: false })).toBe("transcribe");
  });

  it("文字起こし有り・ノート無しなら summarize", () => {
    expect(nextStage({ hasAudio: true, hasTranscript: true, hasNote: false })).toBe("summarize");
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npx vitest run src/pipeline/pendingJobs.test.ts`
Expected: FAIL（`nextStage` が未定義）

- [ ] **Step 3: 最小実装**

`src/pipeline/pendingJobs.ts`:
```ts
/** 各録音フォルダで「次にやること」。skip は対象外または完了。 */
export type NextStage = "transcribe" | "summarize" | "skip";

/**
 * フォルダ内のファイル構成から次の処理段階を判定する。
 * 段階の区切りは整形済みの `文字起こし.txt`（有れば文字起こしは完了）に置く。
 */
export function nextStage(files: {
  hasAudio: boolean;
  hasTranscript: boolean;
  hasNote: boolean;
}): NextStage {
  if (!files.hasAudio) return "skip";
  if (files.hasNote) return "skip";
  if (files.hasTranscript) return "summarize";
  return "transcribe";
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npx vitest run src/pipeline/pendingJobs.test.ts`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/pipeline/pendingJobs.ts src/pipeline/pendingJobs.test.ts
git commit -m "保留ジョブの段階判定 nextStage を追加" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: 対象フォルダの抽出・整列 `selectPendingDirs`（純関数）

**Files:**
- Modify: `src/pipeline/pendingJobs.ts`
- Test: `src/pipeline/pendingJobs.test.ts`

**Interfaces:**
- Consumes: なし
- Produces:
  - `type DirEntry = { name: string; hasAudio: boolean; hasNote: boolean }`
  - `function selectPendingDirs(entries: DirEntry[], excludeName?: string): string[]`
    （`hasAudio && !hasNote` かつ `name !== excludeName` を、`name` 昇順で返す）

- [ ] **Step 1: 失敗するテストを書く**

`src/pipeline/pendingJobs.test.ts` に追記:
```ts
import { nextStage, selectPendingDirs } from "./pendingJobs.js";

describe("selectPendingDirs", () => {
  const entries = [
    { name: "2026-07-09_1030_録音", hasAudio: true, hasNote: false },
    { name: "2026-07-08_0900_録音", hasAudio: true, hasNote: false },
    { name: "2026-07-07_1500_録音", hasAudio: true, hasNote: true }, // 完了 → 除外
    { name: "空フォルダ", hasAudio: false, hasNote: false }, // 音声なし → 除外
  ];

  it("未完（音声あり・ノートなし）のみを古い順で返す", () => {
    expect(selectPendingDirs(entries)).toEqual([
      "2026-07-08_0900_録音",
      "2026-07-09_1030_録音",
    ]);
  });

  it("録音中フォルダ（excludeName）は除外する", () => {
    expect(selectPendingDirs(entries, "2026-07-09_1030_録音")).toEqual([
      "2026-07-08_0900_録音",
    ]);
  });

  it("対象が無ければ空配列", () => {
    expect(selectPendingDirs([])).toEqual([]);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npx vitest run src/pipeline/pendingJobs.test.ts`
Expected: FAIL（`selectPendingDirs` が未定義）

- [ ] **Step 3: 最小実装**

`src/pipeline/pendingJobs.ts` に追記:
```ts
/** 走査で得た1フォルダ分の要約情報 */
export type DirEntry = { name: string; hasAudio: boolean; hasNote: boolean };

/**
 * 未完フォルダ（音声あり・ノートなし）を古い順（フォルダ名昇順＝タイムスタンプ順）で返す。
 * excludeName は録音中フォルダなど処理対象から外したいものの basename。
 */
export function selectPendingDirs(entries: DirEntry[], excludeName?: string): string[] {
  return entries
    .filter((e) => e.hasAudio && !e.hasNote && e.name !== excludeName)
    .map((e) => e.name)
    .sort();
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npx vitest run src/pipeline/pendingJobs.test.ts`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/pipeline/pendingJobs.ts src/pipeline/pendingJobs.test.ts
git commit -m "保留ジョブ対象の抽出・整列 selectPendingDirs を追加" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: 背景要約の可否 `canSummarizeInBackground`（純関数）

**Files:**
- Modify: `src/pipeline/pendingJobs.ts`
- Test: `src/pipeline/pendingJobs.test.ts`

**Interfaces:**
- Consumes: `EngineName`（`src/config.ts`: `"claude" | "ollama"`）
- Produces: `function canSummarizeInBackground(engine: EngineName, cloudConsent: boolean): boolean`

- [ ] **Step 1: 失敗するテストを書く**

`src/pipeline/pendingJobs.test.ts` に追記:
```ts
import {
  nextStage,
  selectPendingDirs,
  canSummarizeInBackground,
} from "./pendingJobs.js";

describe("canSummarizeInBackground", () => {
  it("claude は同意済みのときだけ背景要約できる", () => {
    expect(canSummarizeInBackground("claude", true)).toBe(true);
    expect(canSummarizeInBackground("claude", false)).toBe(false);
  });

  it("ollama（ローカル）は同意に関係なく背景要約できる", () => {
    expect(canSummarizeInBackground("ollama", false)).toBe(true);
    expect(canSummarizeInBackground("ollama", true)).toBe(true);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npx vitest run src/pipeline/pendingJobs.test.ts`
Expected: FAIL（`canSummarizeInBackground` が未定義）

- [ ] **Step 3: 最小実装**

`src/pipeline/pendingJobs.ts` に追記（先頭付近に import も追加）:
```ts
import type { EngineName } from "../config.js";
```
```ts
/**
 * バックグラウンドで要約まで進めてよいか。
 * claude（クラウド）は未同意だとダイアログを出せないため、同意済みのときだけ。
 * ollama はローカルなので常に可。
 */
export function canSummarizeInBackground(engine: EngineName, cloudConsent: boolean): boolean {
  return engine !== "claude" || cloudConsent;
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npx vitest run src/pipeline/pendingJobs.test.ts`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/pipeline/pendingJobs.ts src/pipeline/pendingJobs.test.ts
git commit -m "背景要約の可否判定 canSummarizeInBackground を追加" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: ジョブメタ `job.json` の読み書きと資料の存在フィルタ

**Files:**
- Create: `src/pipeline/jobMeta.ts`
- Test: `src/pipeline/jobMeta.test.ts`

**Interfaces:**
- Consumes: `atomicWriteFile`（`src/util/files.ts`）
- Produces:
  - `type JobMeta = { recordedAt: string; materialPaths: string[] }`
  - `const JOB_META_FILE = "job.json"`
  - `function readJobMeta(dir: string): Promise<JobMeta>`（無い/壊れていれば `{ recordedAt: "", materialPaths: [] }`）
  - `function writeJobMeta(dir: string, meta: JobMeta): Promise<void>`
  - `function filterExistingPaths(paths: string[]): Promise<string[]>`（実在するファイルのみ残す）

- [ ] **Step 1: 失敗するテストを書く**

`src/pipeline/jobMeta.test.ts`:
```ts
import { describe, it, expect, beforeEach, afterEach } from "vitest";
import { mkdtemp, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import {
  readJobMeta,
  writeJobMeta,
  filterExistingPaths,
  JOB_META_FILE,
} from "./jobMeta.js";

let dir: string;

beforeEach(async () => {
  dir = await mkdtemp(join(tmpdir(), "jobmeta-"));
});
afterEach(async () => {
  await rm(dir, { recursive: true, force: true });
});

describe("writeJobMeta / readJobMeta", () => {
  it("書いた内容をそのまま読み戻せる", async () => {
    const meta = { recordedAt: "2026-07-09T10:30:00.000Z", materialPaths: ["/a.pdf"] };
    await writeJobMeta(dir, meta);
    expect(await readJobMeta(dir)).toEqual(meta);
  });

  it("job.json が無ければ既定値を返す", async () => {
    expect(await readJobMeta(dir)).toEqual({ recordedAt: "", materialPaths: [] });
  });

  it("job.json が壊れていれば既定値を返す", async () => {
    await writeFile(join(dir, JOB_META_FILE), "{壊れたjson", "utf8");
    expect(await readJobMeta(dir)).toEqual({ recordedAt: "", materialPaths: [] });
  });
});

describe("filterExistingPaths", () => {
  it("実在するファイルだけ残す", async () => {
    const exists = join(dir, "slides.txt");
    await writeFile(exists, "x", "utf8");
    const missing = join(dir, "no-such-file.pdf");
    expect(await filterExistingPaths([exists, missing])).toEqual([exists]);
  });

  it("空配列はそのまま空", async () => {
    expect(await filterExistingPaths([])).toEqual([]);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npx vitest run src/pipeline/jobMeta.test.ts`
Expected: FAIL（`./jobMeta.js` を解決できない）

- [ ] **Step 3: 最小実装**

`src/pipeline/jobMeta.ts`:
```ts
import { readFile } from "node:fs/promises";
import { access } from "node:fs/promises";
import { join } from "node:path";
import { atomicWriteFile } from "../util/files.js";

/** 録音フォルダに保存する per-録音 のメタ情報（要約に必要な最小限） */
export type JobMeta = {
  /** 録音時刻（ISO8601）。表示・並び用 */
  recordedAt: string;
  /** 添付された授業資料の絶対パス（無ければ空） */
  materialPaths: string[];
};

export const JOB_META_FILE = "job.json";

const EMPTY_META: JobMeta = { recordedAt: "", materialPaths: [] };

/** job.json を読む。無い/壊れている場合は既定値（要約は資料なしで続行できる）。 */
export async function readJobMeta(dir: string): Promise<JobMeta> {
  try {
    const raw = await readFile(join(dir, JOB_META_FILE), "utf8");
    const parsed = JSON.parse(raw) as Partial<JobMeta>;
    return {
      recordedAt: typeof parsed.recordedAt === "string" ? parsed.recordedAt : "",
      materialPaths: Array.isArray(parsed.materialPaths) ? parsed.materialPaths : [],
    };
  } catch {
    return { ...EMPTY_META };
  }
}

/** job.json を atomic に書く。 */
export async function writeJobMeta(dir: string, meta: JobMeta): Promise<void> {
  await atomicWriteFile(join(dir, JOB_META_FILE), JSON.stringify(meta, null, 2));
}

/** 実在するファイルパスのみ残す（移動・削除済みの資料は黙ってスキップ）。 */
export async function filterExistingPaths(paths: string[]): Promise<string[]> {
  const results: string[] = [];
  for (const p of paths) {
    try {
      await access(p);
      results.push(p);
    } catch {
      // 存在しない資料はスキップ
    }
  }
  return results;
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npx vitest run src/pipeline/jobMeta.test.ts`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/pipeline/jobMeta.ts src/pipeline/jobMeta.test.ts
git commit -m "job.json 読み書きと資料の存在フィルタを追加" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: 逐次ジョブ処理 `processPendingJobs`（走査→文字起こし→要約）

Electron 依存を持たない中核ロジック。純関数（Task 1〜3）と jobMeta（Task 4）、既存パイプラインを組み合わせる。ロジック中核は既にテスト済みのため、本タスクの受け入れは型チェック＋既存テスト緑＋下記の手動スモークで行う。

**Files:**
- Modify: `src/pipeline/pendingJobs.ts`

**Interfaces:**
- Consumes:
  - `nextStage`, `selectPendingDirs`, `canSummarizeInBackground`, `DirEntry`（同ファイル）
  - `readJobMeta`, `filterExistingPaths`（`./jobMeta.js`）
  - `transcribe`（`./transcribe.js`）: `transcribe(audioPath, { model, language, initialPrompt, outputDir }): Promise<string>`
  - `removeHallucinationLoops`（`./clean.js`）: `removeHallucinationLoops(text, { maxRepeats }): string`
  - `summarizeTranscript`（`../run.js`）: `summarizeTranscript(cleaned, summarizer, options): Promise<string>`
  - `makeSummarizer`（`../summarizerFactory.js`）
  - `loadMaterials`（`./material.js`）
  - `buildInitialPrompt`（`../config.js`）, `AppConfig`（`../config.js`）
  - `atomicWriteFile`（`../util/files.js`）
- Produces:
  - `const AUDIO_FILE = "録音.wav"`, `const TRANSCRIPT_FILE = "文字起こし.txt"`, `const NOTE_FILE = "ノート.md"`
  - `type ProcessDeps = { outputRoot: string; config: AppConfig; excludeDir?: string; notify: (title: string, body: string) => void }`
  - `function processPendingJobs(deps: ProcessDeps): Promise<void>`

- [ ] **Step 1: 実装を書く**

`src/pipeline/pendingJobs.ts` の import 群に追加:
```ts
import { existsSync } from "node:fs";
import { readFile, readdir } from "node:fs/promises";
import { basename, join } from "node:path";
import { buildInitialPrompt, type AppConfig } from "../config.js";
import { atomicWriteFile } from "../util/files.js";
import { transcribe } from "./transcribe.js";
import { removeHallucinationLoops } from "./clean.js";
import { summarizeTranscript } from "../run.js";
import { makeSummarizer } from "../summarizerFactory.js";
import { loadMaterials } from "./material.js";
import { readJobMeta, filterExistingPaths } from "./jobMeta.js";
```
（`import type { EngineName } from "../config.js";` は Task 3 で追加済み。`AppConfig` を同じ import に足すか別行で追加してよい。）

同ファイル末尾に追加:
```ts
export const AUDIO_FILE = "録音.wav";
export const TRANSCRIPT_FILE = "文字起こし.txt";
export const NOTE_FILE = "ノート.md";

export type ProcessDeps = {
  outputRoot: string;
  config: AppConfig;
  /** 録音中フォルダの絶対パス（処理対象から除外する） */
  excludeDir?: string;
  notify: (title: string, body: string) => void;
};

/** 保存先ルートを走査し、未完フォルダを1件ずつ処理する（fs 走査部）。 */
async function scanEntries(outputRoot: string): Promise<DirEntry[]> {
  if (!existsSync(outputRoot)) return [];
  const dirents = await readdir(outputRoot, { withFileTypes: true });
  const entries: DirEntry[] = [];
  for (const d of dirents) {
    if (!d.isDirectory()) continue;
    const dir = join(outputRoot, d.name);
    entries.push({
      name: d.name,
      hasAudio: existsSync(join(dir, AUDIO_FILE)),
      hasNote: existsSync(join(dir, NOTE_FILE)),
    });
  }
  return entries;
}

/**
 * 未完の録音フォルダを古い順に1件ずつ処理する。
 * 文字起こし（ローカル）→ 要約（ネット必須）の順。段階の区切りは `文字起こし.txt`。
 * 1件が失敗しても保留のまま残し、通知して次へ進む（全体は止めない）。
 */
export async function processPendingJobs(deps: ProcessDeps): Promise<void> {
  const { outputRoot, config, excludeDir, notify } = deps;
  const entries = await scanEntries(outputRoot);
  const names = selectPendingDirs(entries, excludeDir ? basename(excludeDir) : undefined);

  for (const name of names) {
    const dir = join(outputRoot, name);
    const audioPath = join(dir, AUDIO_FILE);
    const transcriptPath = join(dir, TRANSCRIPT_FILE);
    const notePath = join(dir, NOTE_FILE);

    try {
      const stage = nextStage({
        hasAudio: existsSync(audioPath),
        hasTranscript: existsSync(transcriptPath),
        hasNote: existsSync(notePath),
      });
      if (stage === "skip") continue;

      // 1) 文字起こし（未なら実行）。ローカル・ネット不要。
      let cleaned: string;
      if (stage === "transcribe") {
        const raw = await transcribe(audioPath, {
          model: config.whisperModel,
          language: config.language,
          initialPrompt: buildInitialPrompt(config.vocabulary),
          outputDir: dir,
        });
        cleaned = removeHallucinationLoops(raw, { maxRepeats: config.maxRepeats });
        await atomicWriteFile(transcriptPath, cleaned);
      } else {
        cleaned = await readFile(transcriptPath, "utf8");
      }

      // 2) 要約（背景で可なら実行）。不可なら文字起こしまでで保留。
      if (!canSummarizeInBackground(config.engine, config.cloudConsent)) {
        notify("文字起こしを保存しました", `要約は未同意のため保留中: ${name}`);
        continue;
      }

      const materialPaths = await filterExistingPaths((await readJobMeta(dir)).materialPaths);
      const materials = materialPaths.length > 0 ? await loadMaterials(materialPaths) : "";
      const summarizer = makeSummarizer({ engine: config.engine, model: config.model });
      const note = await summarizeTranscript(cleaned, summarizer, {
        maxCharsPerChunk: config.maxCharsPerChunk,
        vocabulary: config.vocabulary,
        materials,
      });
      await atomicWriteFile(notePath, note);
      notify("ノート完成", notePath);
    } catch (err) {
      notify("後処理に失敗しました（保留のまま残します）", err instanceof Error ? err.message : String(err));
    }
  }
}
```

- [ ] **Step 2: 型チェックと既存テストが通ることを確認**

Run: `npm run typecheck && npx vitest run`
Expected: PASS（型エラーなし、全既存テスト緑）

- [ ] **Step 3: 手動スモーク（再開処理の確認）**

一時的な保存先で「文字起こし済み・ノート未」の状態を作り、要約段だけ走ることを確認する:
```bash
mkdir -p /tmp/ln-smoke/2026-01-01_0000_録音
# ダミー音声（要約段だけ走らせるので中身は使わない）
touch /tmp/ln-smoke/2026-01-01_0000_録音/録音.wav
printf 'これはテスト用の文字起こしです。十分な長さの本文を入れておきます。' \
  > /tmp/ln-smoke/2026-01-01_0000_録音/文字起こし.txt
```
Node で `processPendingJobs({ outputRoot: "/tmp/ln-smoke", config: <ollama 設定 or cloudConsent:true>, notify: console.log })` を呼び、`ノート.md` が生成されることを確認する（engine が使えない環境ではこのスモークは省略可。型チェック緑で受け入れとする）。

- [ ] **Step 4: コミット**

```bash
git add src/pipeline/pendingJobs.ts
git commit -m "逐次ジョブ処理 processPendingJobs を追加" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: main.ts にガード付きランナーと起動時／復帰トリガを配線

**Files:**
- Modify: `src/electron/main.ts`

**Interfaces:**
- Consumes: `processPendingJobs`, `ProcessDeps`（`../pipeline/pendingJobs.js`）
- Produces（同ファイル内で使用）:
  - `let activeRecordingDir: string | null`（Task 7 と共有。本タスクで宣言する）
  - `function runPendingJobsGuarded(): Promise<void>`（多重実行を coalesce するガード）

- [ ] **Step 1: import と状態変数を追加**

`src/electron/main.ts` の electron import に `powerMonitor` を追加（[1〜12 行目](../../src/electron/main.ts) の import 群）:
```ts
import {
  app,
  Tray,
  Menu,
  Notification,
  shell,
  nativeImage,
  BrowserWindow,
  ipcMain,
  dialog,
  screen,
  powerMonitor,
} from "electron";
```
pendingJobs の import を追加（既存の `import { processTranscript } from "../run.js";` 付近）:
```ts
import { processPendingJobs } from "../pipeline/pendingJobs.js";
```
状態変数を追加（`let currentJob ...` 付近, [45 行目](../../src/electron/main.ts) 付近）:
```ts
/** 録音中フォルダ（背景処理の対象から除外する）。 */
let activeRecordingDir: string | null = null;
/** 背景ジョブ処理の多重実行ガード。 */
let jobRunning = false;
let jobRerun = false;
```

- [ ] **Step 2: ガード付きランナーを追加**

`registerIpc` の前あたりに関数を追加:
```ts
/**
 * 保留ジョブ処理を多重起動させずに走らせる。
 * 実行中に再要求（起動時＋復帰が重なる等）が来たら、現在の周回終了後にもう一度走査する。
 */
async function runPendingJobsGuarded(): Promise<void> {
  if (jobRunning) {
    jobRerun = true;
    return;
  }
  jobRunning = true;
  try {
    do {
      jobRerun = false;
      await processPendingJobs({
        outputRoot: config.outputRoot,
        config,
        excludeDir: activeRecordingDir ?? undefined,
        notify,
      }).catch((err) => notify("後処理でエラー", errorMessage(err)));
    } while (jobRerun);
  } finally {
    jobRunning = false;
  }
}
```

- [ ] **Step 3: 起動時と復帰時にキック**

`app.whenReady().then(...)` の中、`void startupDependencyCheck();`（[456 行目](../../src/electron/main.ts) 付近）の直後に追加:
```ts
  // 起動時に未処理の録音を片付ける
  void runPendingJobsGuarded();
  // スリープ復帰時にも走らせる（次の教室でフタを開けた瞬間に文字起こしが進む）
  powerMonitor.on("resume", () => void runPendingJobsGuarded());
```

- [ ] **Step 4: 型チェックとビルドを確認**

Run: `npm run typecheck && npm run build`
Expected: PASS（型エラー・ビルドエラーなし）

- [ ] **Step 5: コミット**

```bash
git add src/electron/main.ts
git commit -m "起動時・スリープ復帰時に保留ジョブ処理を走らせる" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7: `stopAndProcess` を非ブロッキング化（保存のみ→背景処理へ）

**Files:**
- Modify: `src/electron/main.ts`

**Interfaces:**
- Consumes: `writeJobMeta`（`../pipeline/jobMeta.js`）, `runPendingJobsGuarded`・`activeRecordingDir`（Task 6）, 既存 `ensureCloudConsent`
- Produces: なし（`stopAndProcess` の挙動変更）

- [ ] **Step 1: jobMeta の import を追加**

`src/electron/main.ts` の import 群に追加:
```ts
import { writeJobMeta } from "../pipeline/jobMeta.js";
```

- [ ] **Step 2: 録音開始時に activeRecordingDir を記録**

`startRecording` 内、`currentJob = { outDir, audioPath, materialPaths: pendingMaterials };`（[293 行目](../../src/electron/main.ts) 付近）の直後に追加:
```ts
  activeRecordingDir = outDir;
```

- [ ] **Step 3: `stopAndProcess` を保存のみに置き換える**

[325〜390 行目](../../src/electron/main.ts) の `stopAndProcess` 全体を次に置き換える:
```ts
async function stopAndProcess(): Promise<void> {
  if (state !== "recording" || !recorder || !currentJob) return;
  const { outDir, materialPaths } = currentJob;

  if (elapsedTimer) {
    clearInterval(elapsedTimer);
    elapsedTimer = null;
  }
  setState("processing");

  try {
    await recorder.stop();
    recorder = null;
    await stopLiveCaption();
    await rm(join(outDir, "live"), { recursive: true, force: true }).catch(() => {});

    // 要約の同意（claude 初回のみ。ユーザーがその場にいるので一瞬）。
    // 拒否時は cloudConsent が false のままなので、背景では文字起こしまでで保留になる。
    await ensureCloudConsent();

    // 背景要約に使う情報だけ保存（語彙・エンジン等は処理時の現在設定を使う）。
    await writeJobMeta(outDir, {
      recordedAt: new Date().toISOString(),
      materialPaths,
    });

    notify("録音を保存しました", "文字起こし・要約はバックグラウンドで進みます（フタを閉じてOK）");
  } catch (err) {
    notify("録音の保存に失敗しました", errorMessage(err));
  } finally {
    recorder = null;
    currentJob = null;
    activeRecordingDir = null;
    destroyCaptionWindow();
    if (liveTranscriber) {
      void liveTranscriber.stop().catch(() => {});
      liveTranscriber = null;
    }
    setState("idle");
  }

  // idle に戻してから背景処理をキック（その場で待てば従来どおり完成する）。
  void runPendingJobsGuarded();
}
```

- [ ] **Step 4: 未使用になった import を整理**

`processTranscript`（`../run.js`）が main.ts から使われなくなる。`transcribe`（`../pipeline/transcribe.js`）・`makeSummarizer`・`loadMaterials`・`atomicWriteFile` の `atomicWriteFile` が他で使われていないか確認する。
Run: `npx eslint src/electron/main.ts` または `npm run typecheck`（`noUnusedLocals` 有効時は型チェックで検出）
未使用になった import 文（`processTranscript`、`transcribe`、`makeSummarizer`、`loadMaterials`、そして他で未使用なら `atomicWriteFile`）を削除する。`formatTimestamp` は `startRecording` で使用継続、`buildInitialPrompt` はライブ字幕で使用継続のため残す。

- [ ] **Step 5: 型チェックとビルドを確認**

Run: `npm run typecheck && npm run build`
Expected: PASS（未使用 import が残っていれば `noUnusedLocals` で FAIL するので削除して再実行）

- [ ] **Step 6: 手動スモーク（実機・任意）**

`npm run app` で起動 →「● 録音開始」→ 数秒話す →「■ 停止してノート化」。停止後すぐにメニューが idle（「● 録音開始」表示）へ戻り、しばらくして「ノート完成」通知が出て `ノート.md` が生成されることを確認する。続けて、保存フォルダの `ノート.md` を消してアプリ再起動 → 起動時トリガでノートが再生成されることを確認する。

- [ ] **Step 7: コミット**

```bash
git add src/electron/main.ts
git commit -m "停止処理を非ブロッキング化し背景ジョブへ委譲" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 8: README に挙動を追記

**Files:**
- Modify: `README.md`

**Interfaces:** なし

- [ ] **Step 1: 挙動の追記**

`README.md` のメニューバーアプリ節、`完了すると通知が出てノートが開く。…` の段落（[32 行目](../../README.md) 付近）の後に次を追記:
```markdown

停止後は**すぐにフタを閉じて移動して構わない**。文字起こし（ローカル）と要約はバックグラウンドで進み、途中で中断（スリープ・WiFi 切替・終了）しても失われない。未完の録音は**次回起動時・スリープ復帰時に自動で続きから処理**される（文字起こしはネット不要なので復帰後すぐ完了し、要約はネットが安定したときに再試行される）。要約に claude を使う場合、初回の停止時に出るクラウド送信の同意（「今後確認しない」）をしておくと、以降の背景要約が自動で走る。
```

- [ ] **Step 2: 差分確認**

Run: `git diff README.md`
Expected: 上記段落が追加されている

- [ ] **Step 3: コミット**

```bash
git add README.md
git commit -m "README に停止後の背景処理・自動再開の挙動を追記" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 実装後の確認（全体）

- [ ] `npm run typecheck` が緑
- [ ] `npx vitest run` が緑（新規: `pendingJobs.test.ts` / `jobMeta.test.ts` を含む）
- [ ] `npm run build` が成功
- [ ] （実機）停止→即 idle 復帰→背景でノート生成、再起動での自動再開を確認
